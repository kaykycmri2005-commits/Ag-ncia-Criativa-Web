@use 'variaveis';
@use 'mixins';
@use 'base';
@use 'layout';
@use 'componentes';

@media (max-width: 768px) {
  .header__nav-list {
    flex-direction: column;
    align-items: center;
  }

  .banner {
    height: 60vh;
    padding: 1rem;
  }
}
